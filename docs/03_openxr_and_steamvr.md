# OpenXR and Runtime

## OpenXR
OpenXR is used as a standard interface to access VR hardware in an abstracted way.

## Runtime
SteamVR is used as the active runtime.

## Relevant Features
- Eye tracking support
- Device access via profiles such as EyeGaze

## Considerations
- The runtime must be correctly configured
- Some features depend on hardware-specific support